<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TestAndroidApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="android:windowActionBar">false</item>
        <item name="colorPrimary">@color/yellow</item>
        <item name="titleTextColor">@color/white</item>
    </style>
    <style name="Theme.EditorActivity" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">false</item>
<!--        <item name="colorPrimary">@color/yellow</item>-->
<!--        <item name="titleTextColor">@color/white</item>-->
    </style>
    <style name="editorInputView" parent="Theme.AppCompat.Light">
<!--        <item name="colorControlNormal">@color/colorPrimary</item>-->
        <item name="colorControlActivated">@android:color/holo_orange_dark</item>
    </style>

</resources>